#!/usr/bin/env bash

set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install --path .bundle

export NMAX_PATH=$(echo $(echo $(pwd); echo '/bin/nmax') | sed '/^$/d;s/[[:blank:]]//g')
sudo echo "alias nmax='$NMAX_PATH'" >> ~/.bashrc

echo "Alias 'nmax' added!"
echo "Please restart terminal!"
